<Application x:Class="WpfDummyApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- Colors -->
        <SolidColorBrush x:Key="SidebarBg" Color="#1E1E2F"/>
        <SolidColorBrush x:Key="SidebarHover" Color="#2D2D44"/>
        <SolidColorBrush x:Key="AccentColor" Color="#6C5CE7"/>
        <SolidColorBrush x:Key="ContentBg" Color="#F5F6FA"/>
        <SolidColorBrush x:Key="BoxBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextMuted" Color="#A0A0B2"/>

        <!-- Sidebar menu button style -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Margin="10,3">
                            <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dashboard box style -->
        <Style x:Key="DashboardBoxStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BoxBg}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="1" Opacity="0.15" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
